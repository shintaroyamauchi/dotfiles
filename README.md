# dotfiles

home ディレクトリ直下にダウンロード
以下を実行してシンボリックリンクをはる．

```bash
ln -s ~/dotfiles/nvim ~/.config/nvim
ln -s ~/dotfiles/tmux ~/.config/tmux
ln -s ~/dotfiles/git ~/.config/git
ln -s ~/dotfiles/.zshrc ~/.zshrc
ln -s ~/dotfiles/.p10k.zsh ~/.p10k.zsh
```
